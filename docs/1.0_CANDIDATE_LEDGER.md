# Current 1.0 candidate completion ledger

The delivery destination is `origin/main`. Completion requires a green hosted
Cinemarr CI run on the exact final commit, matching local/hosted artifacts,
current acceptance evidence and consolidated branches. No tagging, publication
or release creation is authorized. An intermediate push or passing subset does
not complete this goal. Repeated test failures remain implementation work.

## Current work: Plex segment timestamps

Candidate `7266eacd6e3c3b412f773337b363bfa80cf0ceb3` passed two identical
forced builds, sixteen artifact inspections, all ten GameTests, all sixty-two
local runtime cases and two fresh legacy main runs. The builds reported 775
tests, five skips and no failures. Windows x64 and emulated Linux ARM64 decoder
evidence was bound to its exact packaged payloads. Thirteen of fourteen real-Plex
cases passed automation; eight had scoped acceptance before a new content-timing
defect was confirmed. These receipts remain historical evidence for those bytes.

Direct NeoForge 1.21.1 display review found that the replaced 240p TV showed a
different aerial image from its siblings. Independent matching against the
original movie located that image near 25.9 seconds, although its renderer
reported 32.9 seconds. The other TVs showed content near their reported times.
A diagnostic Plex request at offset 31 returned `00003.ts`, the 24–32 second
segment. The parser incorrectly assigned it a 31-second start, shifting the
replacement by seven seconds. Close agreement between reported clocks and a
passing mixed-audio comparison did not establish content synchronization.

The repair retains cumulative VOD segment boundaries after dropping pre-seek
entries. Already seek-relative windows retain their existing handling. Four
regression checks failed against the candidate; the repaired core suite reports
317 tests, one skip and no failures. Replaying the actual retained Plex playlist
through compiled Java code changed the first two starts from 31/39 seconds to
24/32 seconds. This is development evidence, not packaged runtime acceptance.
The [development manifest](RELEASE_PLEX_SEGMENT_ORIGIN_DEVELOPMENT_20260925.json)
binds the observations, source files and test evidence.

The first full repair build stopped on a legacy adapter test that still expected
a 290-second segment start for segment 36 of an eight-second VOD playlist.
The test now requires the original 288-second boundary, its eight-second duration,
the next 296-second boundary and the four retained segments. All three focused
legacy adapter tests pass. The failed full build remains preserved; another full
development build and packaged playback verification remain required.

The exact-candidate [hosted run 36089558294](https://github.com/StonyTark1117/Cinemarr/actions/runs/36089558294)
finished with sixteen successful jobs, Forge 1.20.2 client initialization failed,
and the aggregate release gate skipped. Legacy real-Plex controls also failed
during DiscPanel/RCON scene preparation before client launch. Both investigations
remain open. Earlier green CI and artifact parity do not certify this candidate
or the repair.

Before freezing another candidate, verify repaired packaged modern and legacy
playback against original movie content after quality changes, seek and resume.
Then repeat the full original frozen-commit certification scope: two builds,
all artifacts/GameTests, the complete local matrix and supplements, two fresh
legacy runs, fourteen exact-artifact real-Plex cases, native payload binding,
direct original-image and physical-audio review, configured scans and cleanup,
all eighteen green hosted jobs and downloaded bundle parity. No release tag or
publication is authorized.

## Historical checkpoint: bounded audio recovery

Frozen candidate `ac25666cb05bd13abef8cbcbe5c6cb870e9ebc1c` passed
[hosted CI run 36065898806](https://github.com/StonyTark1117/Cinemarr/actions/runs/36065898806):
all eighteen jobs succeeded, and all eighteen downloaded bundle files match two
forced local builds. All sixteen JAR inspections and ten GameTests passed.
All fourteen exact-artifact real-Plex cases now have scoped acceptance and
configured scans. Windows x64 and emulated Linux ARM64 decoder acceptance is
bound to these JARs. The [checkpoint manifest](RELEASE_CERTIFICATION_AC25666.json)
records bundle and evidence-receipt hashes.

Its full local attempt exited unsuccessfully after all twenty-one main profiles.
Twenty completed automation; NeoForge 1.21.1 failed physical program-audio
capture after reconnect. The follower recording began with 5.362 seconds of
silence while video continued. Drift recovery had discarded PCM already handed
to the Java/OpenAL queues, leaving only future decoded samples. The initiating
125 ms clock drift remains unexplained. Seventeen of sixty-two local cases have
scoped acceptance, including eleven main cases and all six terminal, pressure,
and network-fault representatives. Remaining supplements, evidence review, and
the second fresh legacy run are incomplete. All 947 source inputs and eighteen
bundle files stayed unchanged. Terminal cleanup found no owned processes or
private displays; all forty-two game/RCON ports checked were closed.

The current repair retains original PCM within an eight-second duration/byte
budget and 1,024-frame bound: at 48 kHz stereo, at most 1,536,000 bytes per
stream. Recovery temporarily restores these frames ahead of pending audio;
existing decoder backpressure stops further polling until the queue drains.
Terminal drain and identity changes discard the history. Modern sound-engine
closure preserves history until its owner can recover it; legacy context reload
continues to discard native IDs without calling the destroyed OpenAL context.

The baseline loss was reproduced against actual compiled modern and legacy
classes. The repair passed forty focused tests, both 26.x adapter compiles,
and legacy Java 8 bytecode compilation. A complete development build passed
all sixteen artifact inspections and ten GameTests; its reports contain 775
tests, five skips, and no failures or errors, including thirteen new recovery
regressions. The initial legacy test syntax failure and two diagnostic harness
failures remain preserved; the successful runs followed explicit corrections.

Both complete development main profiles then passed with the maintained
controls, display-feature, reconnect, and shutdown checks enabled. Continuous
physical PCM around a real resource/sound reload measured interruptions of
1.70 seconds on NeoForge 1.21.1 and 3.08 seconds on Forge 1.7.10. Recovered
lag/correlation was -10 ms/0.994935 and +20 ms/0.995855 respectively. Final
eight-second captures after reconnect measured +10 ms/0.996875 and
+10 ms/0.999866, with program audio from the first sample on both clients.
Legacy also completed two additional resource/sound reloads per client. Four
NeoForge and six legacy screenshots were directly reviewed; the other 180
feature images remain unreviewed. These are development results, not final
runtime acceptance. No thresholds were relaxed, and the original spontaneous
clock-drift trigger remains unexplained.

Both runs retained all 950 source inputs and eighteen bundle hashes, exited
normally, and passed their scoped cleanup audits. Configured scans passed for
the repaired source/build and both retained main-profile evidence sets. The
[development manifest](RELEASE_AUDIO_RECOVERY_DEVELOPMENT_20260924.json)
binds these results and their limits. Freeze the repaired commit next; two
forced builds and the complete original final-commit certification scope remain
required. The prior checkpoint's hosted success does not certify this repair.

The older Forge 1.20.2 initialization failure did not recur in this candidate's
full main case; its original cause remains unresolved. The older checkpoint
below is retained as historical evidence and does not certify current changes.

## Verified checkpoint: September 24

Product commit `5e83b1e7bf11d2273d334b5b0c1e0b37711a1b58` passed
[hosted CI run 35926936307](https://github.com/StonyTark1117/Cinemarr/actions/runs/35926936307):
all eighteen jobs succeeded. Two forced local builds produced identical
sixteen-JAR bundles, and all eighteen downloaded bundle files match local bytes.
All ten GameTests passed. The [checkpoint manifest](RELEASE_CERTIFICATION_20260924.json)
records artifact hashes and evidence-receipt hashes.

Local review accepted 61 of 62 required cases, plus the second fresh legacy run.
The original full matrix **failed** on Forge 1.20.2 client initialization; its
later diagnostic pass does not resolve that failure. All 21 independent-display
cases and every capacity, loader, terminal, pressure and network supplement have
scoped acceptance, including original-image and captured-audio review.

Windows x64 and emulated Linux ARM64 decoder acceptance passed six runs across
three exact packaged core variants bound to all sixteen JARs. Both guests are
off. This proves decoder functionality, not full Minecraft clients or physical
ARM performance. Exact-JAR legacy Plex controls passed with 96 reviewed images,
eight recomputed sparse-frame checks, restart/reload/reconnect evidence and
10 ms measured audio lag. The remaining thirteen Plex cases are incomplete.
Quilt stopped when rain changed the background between sparse-hole captures;
all 48 video-color samples matched, but five hole samples differed. The failed
attempt remains evidence and is not accepted.

The harness now prepares clear weather in stopped saves and restores only the
original weather fields into the current save. It supports legacy/modern
`level.dat` and 26.2's separate weather file. Focused tests and read-only tests
on all four saved formats pass; fresh live acceptance remains required.
Redaction now includes the probe clients' logged addresses and records changed
file hashes. The original legacy scan found a deployment endpoint in two server
logs, with no configured credential matches. A separately preserved, hash-mapped
sanitized export passes the configured scan. Local and native scan scopes also
passed; final scan and cleanup scopes remain open.

These are checkpoint results. The commit containing the harness and record
updates still needs its own complete local gate, hosted CI and artifact parity.
No release-ready claim, tag or publication follows from this checkpoint.

## Historical consolidation and repairs

- Local main `2e72e7b` has the same tree as candidate ancestor `eb8cb36`.
  Its history was merged without changing the candidate tree.
- Remote audit branch `c8fc3f4` contains previously omitted UI, persistence,
  launcher, queue-observer and test-fixture fixes. These have been combined with
  the newer independent-stream, memory, native-lifetime and readiness fixes.
- Both the prior audit and Jammarr contain the Loom nested-Xvfb correction.
  Jammarr run `33898240759` and Cinemarr audit run `34907274193` are historical
  reference evidence, not certification of this combined candidate.
- Downloaded failed-run evidence: `build/ci-investigation/35715733727/`.
  NeoForge's logs show queue advancement followed immediately by preparation
  and playing updates; a latest-row-only observer lost the advance event.
- The recovered sparse-display feature fixture and newer multi-TV capacity
  fixture now have distinct acceptance switches. Both suites remain required.

## Earlier repair evidence

The first consolidated run is [35730737095](https://github.com/StonyTark1117/Cinemarr/actions/runs/35730737095)
on `a742e7d`. It is failed evidence, not certification. Downloaded client crash
reports identify missing overlay/normal vertex elements in the Detailed shader.
The shared emitters now supply those fields, with a real BufferBuilder regression.
Fabric logs also prove a nested `xvfb-run`; the acceptance override now addresses
the task explicitly and locks its value. Local CI-mode configuration reports
`useXvfb=false`.

Local development tests cover pending display-editor state, invalid-input
feedback, private-window diagnostics and early client-crash detection. A local
runtime attempt overlapped a full build and failed reading a rewritten core JAR;
that run remains failed evidence. Current builds and runtimes use separate,
source-frozen worktrees and output roots so their generated JARs cannot race.

The subsequent `9520f6d` run (`35732323882`) reached owner controls and
exposed two observer defects: a handled Netty initialization trace matched a
fatal-error check, and paused-frame retention compared timeline and stream
revisions as one value. Both corrections passed focused regressions and replay
against the failed logs. Run `35733802147` on `0269474` progressed through main
runtime gates and exposed the recovered two-stream override being applied to
the independent three-TV scene. That override is now scoped to the feature
fixture; explicit capacity tests retain their configured two-stream limit.

The local `consolidation-build.log` completed all 97 tasks, inspected all sixteen
artifacts and passed all ten GameTests. It is development verification: repairs
were made during that build. Fresh immutable build and runtime evidence remains
required. Local Fabric attempts also exposed unreliable Ctrl+A automation; the
field probe now uses the existing independent-display probe's paced editing keys.
An additional local attempt was invalidated by a production-runtime environment
setting and an in-flight shell edit. Its failure remains recorded. Runtime input
hashes are now recorded before and after each local attempt, and source edits and
builds wait for all active local runtime processes to exit.

Run `35736410639` on `da74403` exposed an omitted independent-display setup
call in the Fabric 26.2 server adapter. The other modern adapters already invoke
that shared setup. This adapter now does too. The same run also contains an
unresolved Fabric 26.1.2 native pthread abort and a NeoForge 1.20.1 connection
stall following an OpenAL timeout. A frozen local Fabric 26.1.2 diagnostic run
completed display, seek, reload and reconnect without reproducing the abort;
it does not close that investigation. Hosted native cores now produce stack
traces, and failed client teardown retains bounded, owned-JVM thread dumps
before processes disappear. Raw core memory, command lines and environments
are excluded from uploaded diagnostic evidence. Local canaries verified original
JDK resolution and exclusion of an unrelated JVM from thread capture.

Frozen local `da74403` Fabric 1.20.1 controls/features and NeoForge 1.21.1
independent-display runs passed with unchanged source receipts. Direct review
of the NeoForge paused pixel-layout and reconnect captures showed the expected
coarse and detailed displays. These are scoped development results; final
candidate matrix, artifact parity and remaining direct reviews stay open.

The first diagnostics push (`463509d`, run `35741219581`) failed a shell-source
assertion that still expected cleanup without its new failure-status argument.
That assertion now follows the failure-aware call; the entire hosted manifest
verification sequence passed in an isolated copy before applying the correction.
The frozen local `fabric-262-display-repair-01` run passed all three-TV display,
seek, reload, reconnect and clean-shutdown checks with unchanged source inputs.
An isolated Java 25 decoder diagnostic also completed 1,200 concurrent audio/video
decodes and 600 cancelled retirement tasks without reproducing the native abort.
These results narrow the investigation; they do not replace final certification.

Run `35742427686` on `289d7a6` is still in progress. Its manifest passed and
its runtime results remain diagnostic until complete. The frozen legacy main
run on that commit passed. Forced-build attempts on `289d7a6` and `eb1dc06`
were stopped when subsequent harness changes made their source identities
obsolete; their partial logs are retained and neither certifies the candidate.

Jammarr's green hosted runtime suite uses a private D-Bus, PipeWire,
WirePlumber and PipeWire-Pulse graph for each gate. Cinemarr had instead used
the runner's shared PulseAudio daemon and OpenAL through an ALSA bridge with
a 512-frame period. The acceptance harness now starts and owns a private graph
before graphical clients, restores its original environment at teardown, uses
Jammarr's 48 kHz/1024-frame/eight-period OpenAL configuration, and directs
legacy LWJGL OpenAL to its measured Pulse sink. Hosted CI installs the same
audio services and no longer starts a shared daemon. A local preflight proved
the private socket and null-sink path and verified both successful and failed
teardown. The packaged real-Plex and lifecycle gates now explicitly enter the
same graph before creating measured sinks. Full runtime, real-Plex and hosted
results for this change remain open.

The private-graph NeoForge 1.20.1 independent-display diagnostic passed its
full three-TV, reload, reconnect, physical PCM and clean-shutdown checks.
Direct original-image review found distinct coarse/detailed rasters after
reload. Its sink captures carried the expected program tones with 20 ms
calibrated lag. A source-frozen `eb1dc06` Fabric 26.1.2 independent-display
run also passed all three-TV phases and shutdown without the earlier native
abort; physical PCM lag was 20 ms. Its scaled controller and paused/resumed
rasters were directly reviewed. The `0060dea` legacy main run passed with
unchanged-source receipt, direct Pulse sink inputs, 997 Hz capture on both
clients and zero measured lag. These are scoped diagnostics because further
harness changes follow, not substitutes for the final two legacy runs.

Jammarr also routes protocol and command probes through OpenAL's null backend:
those probes test networking and UI, while playback clients retain measured
sinks. Cinemarr now makes the same distinction, with an explicit diagnostic
override. Final hosted and local runtime validation of that change is pending.

Run `35746890575` on `1ac80ff` passed all sixteen hosted runtime jobs and the
aggregate 1.0 gate. Its downloaded bundle exposed a local byte-parity defect:
eleven JARs matched a forced local build, while five Forge/NeoForge JARs differed
only in nested core-JAR ZIP permissions. Gradle 8 had carried the local `0664`
umask into those entries; hosted entries were `0644`. The core archive now fixes
file and directory permissions, and release inspection rejects permission drift
inside nested core JARs. Two forced Gradle 8.8 core builds under `0002` and
`0022` produced the same bytes as the hosted core; a canonicalized NeoForge
archive with that core also matched the hosted JAR exactly. Because this repair
changes the source commit, the green `1ac80ff` run is diagnostic evidence and
all final-commit gates remain open.

Run `35815673171` on `5189e7c` subsequently passed the complete hosted matrix,
and two forced local builds produced identical bundles containing all sixteen
artifacts. Retained real-Plex and native-smoke evidence was bound to those
artifact hashes. The combined local runtime matrix then reproduced intermittent
protocol-client timing failures, so that otherwise-green commit remains
diagnostic evidence rather than the final candidate.

Commit `63d9437` separates client preparation and rejection deadlines and makes
the NeoForge 1.21.1 incompatible-protocol rejection explicit. Its focused local
NeoForge gate observed the exact reason and a clean shutdown. Hosted run
`35848441358` then exposed a separate Forge 1.21.1 infrastructure failure: the
Slime Launcher timed out while downloading an indexed Minecraft sound asset
before the client started. The failure artifact identifies the timed-out object
and contains no product-gate failure.

Commit `07f4c76` adds checksum-verified Forge client-asset prefetching with
bounded retries and an offline regression fixture. A real prefetch verified the
1.21.1 asset index and all 3,888 unique objects by SHA-1 and size. Commit
`605d389` entered hosted run
[`35851086163`](https://github.com/StonyTark1117/Cinemarr/actions/runs/35851086163),
where the Forge 1.21.1 asset preparation and runtime passed. That run exposed a
Fabric 1.20.1 login-boundary defect: the server rejected the mismatched Cinemarr
protocol correctly, but the client registered only the later play-disconnect
event and therefore lost the exact reason before the play listener existed.
The client now observes Fabric's login-disconnect event and records the reason
from that connection. A focused `CI=true` 1.20.1 Fabric gate in
`build/focused-fabric-login-rejection-20260923T1133Z/` observed the exact
`server requires version 11` reason, a normally closed harness-owned window,
and clean shutdown without a lingering process or port. A source-layout
regression preserves the login-stage callback. The final candidate is the
`main` commit containing this ledger and repair. Hosted run
[`35855999521`](https://github.com/StonyTark1117/Cinemarr/actions/runs/35855999521)
proved that correction: Fabric 1.20.1 passed its previously failing main runtime
and advanced through its supplemental gates. The same run then exposed an
independent legacy pressure-gate input defect after its main, display, capacity
and terminal gates passed. The first deliberate F3+T resource reload completed
and advanced video, while the second chord produced no reload event because
the private-X helper did not normalize key state left queued during the first
reload. Resource-reload input now clears stale F3/T state and sends an explicit
T key-down/key-up transition while holding F3, with cleanup on partial delivery
failure. A focused `CI=true` legacy pressure run retained four fresh actions in
`build/focused-legacy-pressure-key-normalization-20260923T1230Z/`: two reloads
per client, each followed by an advancing frame in the unchanged playing
session. That run used the hosted 600-second fixture and was stopped after the
fixture reached its natural end during the later physical A/V check; it proves
the repaired input boundary but does not replace final full-gate evidence.
Hosted and local certification must use the exact final commit containing this
repair; no result from an ancestor can close a row below.

## Required final evidence

Statuses below distinguish verified checkpoint evidence from final completion.
Every requirement must still be satisfied for the frozen final containing commit
and its exact artifacts. Passing subsets do not replace a passing full matrix.

| Requirement | Verified checkpoint status / remaining work |
| --- | --- |
| Core, modern and legacy regressions | Passed in both forced builds at `5e83b1e`; new harness tests pass locally |
| 16 artifacts and 10 GameTests | Passed at `5e83b1e` |
| Two identical forced builds | Passed; eighteen-file bundles identical |
| All 21 main runtime profiles | 20/21 accepted; Forge 1.20.2 initialization failure unresolved |
| Quilt/Mod Menu and minimum Fabric loader | 5/5 each accepted |
| Independent display and capacity | 21/21 display, 4/4 capacity accepted |
| Terminal, pressure and network faults | 2/2 in each group accepted |
| Two fresh legacy runs | Both accepted with direct image and PCM review |
| Complete local release matrix | Failed; complete final-commit pass still required |
| Display UI, sparse rasters, persistence, reload/reconnect | Reviewed for accepted local cases and legacy real Plex; remaining live profiles pending |
| Exact-artifact Plex controls and display | Controls 1/4 accepted; display 0/4; Quilt rain failure retained |
| Plex outage recovery and construction lifecycle | Recovery 0/4, lifecycle 0/2 on this checkpoint |
| Windows x64 / Linux ARM64 decoder | Six runs accepted; three exact core variants bound across all sixteen JARs |
| Teardown and configured scans | Local/native scopes passed; legacy sanitized export passed; final scopes open |
| Hosted matrix and aggregate | **Passed: 18/18 jobs on `5e83b1e`, run 35926936307**; final containing-commit CI required |
| Downloaded hosted bundle parity | **Passed: 18/18 files match both local builds** at the checkpoint |
| Release records and final identity | Checkpoint records updated; final certification remains incomplete |
| Git consolidation | Complete: former branch tips consolidated into main |

The full hardening plan, custom-TV specification and release acceptance checklist
remain normative. New failures reopen their owning requirement. Historical
pause and completion statements in older records do not override this ledger.
